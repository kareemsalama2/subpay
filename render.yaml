services:
  - type: web
    name: subpay
    runtime: node
    plan: free
    buildCommand: ""
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: IMAP_POLL_INTERVAL_SECONDS
        value: "5"
